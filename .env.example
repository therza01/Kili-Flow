# GridPulse Environment Variables

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost/gridpulse/api

# Database Configuration (for PHP backend)
# Update these in api/db.php
DB_HOST=localhost
DB_NAME=gridpulse
DB_USER=root
DB_PASSWORD=

# Optional: Weather API (if implementing real weather)
WEATHER_API_KEY=your_weather_api_key_here

# Optional: WhatsApp API (for real notifications)
WHATSAPP_API_TOKEN=your_whatsapp_token_here